AI agent 하네스 관련 논문리뷰 10편

A. Agentic Harness Engineering 2026/04
==========================================
하네스가 중요한걸 알지만 아직 하네스설계는 수작업이고 이를 자동화하는것은 heterogeneous한 공간(action space)들과 같은 어려움을 겪음

1. 수백만 토큰의 raw trace에서, 막 multi step에 skill.md 쓰고 tool call, subagent, MCP 이렇게 쓰면 "어떤 컴포넌트가 왜 실패했는지" 알기 어려움

2. 컴포넌트 간 강한 결합 → 편집이 불안정
기존 하네스 프레임워크는 prompt/tool/middleware가 얽혀있어서, 하나 건드리면 다른 게 망가짐. 편집의 action space 자체가 불명확

->

"이 문제는 에이전트 능력의 문제가 아니라 관찰가능성의 문제다."
evolution 에이젼트에게 구조화된 컨텍스트와 명확한 action space를 주면, 더 나은 하네스를 찾아낼 수 있다"
<img width="1210" height="727" alt="image" src="https://github.com/user-attachments/assets/40988cbd-d34d-4a47-b029-84669c53c354" />

Method
NexAU에서 시도 - Hermes, 클로드 코드 열화판 오픈소스

7개 컴포넌트 타입, 각각의 파일로 분리:
System prompt (systemprompt.md)
Tool description (.tool.yaml)
Tool implementation (tools/*.py)
Middleware (middleware/*.py)
Skill (skills/*/SKILL.md)
Sub-agent configuration (sub_agents/*/agent.yaml)
Long-term memory (LongTermMEMORY.md)

실제 evolve 프롬프트는 "workspace/ 디렉토리만 당신의 작업 공간입니다. 나머지는 읽기 전용이거나 수정 불가입니다" 이런식

요약:Code 에이젼트가 문제풀고 pass@1 실패시 debug 에이젼트가 trajectory읽고 왜 실패했는지 md파일로 만듬 이제 evolve가 이거 읽고 편집 후 change manifest까지 작성
<img width="850" height="709" alt="image" src="https://github.com/user-attachments/assets/b59df76c-2ca7-4ef7-9458-f8aa9a0f9bb1" />

<img width="895" height="497" alt="image" src="https://github.com/user-attachments/assets/466e5cd2-16c9-448b-a48f-711a0f1d7e3e" />


B. Harnessing Agentic Evolution 2026/05
==========================================
<img width="896" height="454" alt="image" src="https://github.com/user-attachments/assets/c9582599-f847-4a7d-956c-30d8ff29658e" />

<img width="854" height="457" alt="image" src="https://github.com/user-attachments/assets/a2e8304a-3ae7-432f-b9ad-7a2ba7ed6aa8" />

C. The Last Harness You'll Ever Build
==========================================
AI agent가 여러 도메인에 적용될때 painstaking, expert-driven 하네스 엔지니어링이 필요한데 2단계 루프를 통해 이를 자동화하겠다

Inner Loop: Harness Evolution Loop 가 수정하는것

시스템 프롬프트와 작업 프롬프트
에이전트의 역할(identity)과 constraint을 정의하는 시스템 수준 지시사항, 그리고 목표·성공 기준·인컨텍스트 예제를 정의하는 작업 수준 프롬프트.

도구(tools), 스킬(skills), 그리고 그 설명
에이전트가 환경과 상호작용하기 위해 호출할 수 있는 기능들.
(예: 파일 수정, 셸 실행, UI 조작, 웹 검색, MCP 서버 등)

번들된 인프라(Bundled infrastructure)
에이전트에게 제공되는 실행 환경.
(파일 시스템, 샌드박스, 브라우저, 관측/로깅 스택 등)
<img width="849" height="597" alt="image" src="https://github.com/user-attachments/assets/601045f4-3d1e-4a6b-9070-d93be4add0be" />
오케스트레이션 로직(Orchestration logic)
에이전트의 상호작용 루프를 구성하는 제어 흐름.
(서브에이전트 생성, 핸드오프, 모델 라우팅, 피드백 루프, Ralph Loop 같은 continuation 패턴 등)

훅(hooks)과 미들웨어(middleware)
모델 주변에 삽입되는 결정론적 실행 보장 장치.
(컨텍스트 압축, continuation, lint 검사, 검증 루프 등)

모델 설정(Model configurations)
어떤 모델을 사용할지, 추론 파라미터(temperature, 샘플링 전략, 토큰 제한), 그리고 어떤 하위 작업을 어떤 모델이 처리할지 결정하는 모델 라우팅 규칙 등을 포함한다.
<img width="1346" height="499" alt="image" src="https://github.com/user-attachments/assets/6e20640b-43cd-4796-9de9-74ac208b8337" />

수정과정 
1. Agent Worker (W_H)
하네스(H)를 장착한 상태로 task 실행
execute(task) → trace

2. Evaluator (V)
trace를 보고 진단 + 점수 평가
evaluate(trace, task) → (report, score)

3. Evolution (E)
이전 히스토리를 기반으로 harness 수정 및 개선
evolve(history, best_harness) → new_harness

Outer Loop: Meta-Evolution Loop 가 수정하는것

Evolution Agent 프롬프트, Evaluator 프롬프트, evaluator 점수 함수 설계, loop 하이퍼파라미터 등 inner loop를 구성하는 모든 것.
<img width="676" height="790" alt="image" src="https://github.com/user-attachments/assets/4497718a-0aad-40f0-8094-f72a87b79c1d" />

D. Life-Harness
==========================================
4개 레이어가 서로 다른 시점에 끼어듦
trajectory(실행 기록)들을 바탕으로 발전하며,
반복적으로 발생하는 interaction 실패를 재사용 가능한 skill과 같은 intervention으로 변환

시작하기 앞서 failed trajectories 를 수집 생 LLM으로 
ex) AlFWorld에서 Qwen3 4B로 뽑은 traj를 Codex를 통해 실패유형 4개중 한개로 매핑 

1. 행동실패 
의도는 맞는데 syntax 같은것 때문에 실행이 안됨
tool_call 대신 그냥 텍스트로 함수 적음
take_action({...}) 이렇게 채팅에 써버림

JSON 형식 깨짐
함수 이름 틀림
required argument 빠짐
argument 타입 틀림
<img width="1233" height="483" alt="image" src="https://github.com/user-attachments/assets/fd699e56-63fa-486b-a25c-355808ff98b2" />

2. 환경 Contract Mismatches
여기서는 tool call 자체는 실행 가능함
근데 “환경 규칙”을 잘못 이해함
예: ["apple", "banana"] 해야되는데 The answer is apple and banana

finish tool 쓰면 안 되는데 사용
answer 제출 전에 중간 tool 먼저 써야 하는데 생략
search tool을 이상하게 사용
형식은 JSON 맞는데 의미가 틀림

3. Trajectory Degeneration
이건 개별 action은 실행 잘 됨, 근데 긴 실행 과정에서 전략이 망가짐
nothing happens 가 계속되는데도 수정 X

같은 행동 반복
같은 페이지 계속 방문
search만 계속 반복
look / inventory 무한 반복
상태 변화 없는데 계속 retry
budget exhausted

4. Residual Reasoning Failures
위의 3개에 안속하면 이제 다 여기, 대부분 모델의 사고(reasoning)가 틀림

계산 실수
SQL 논리 틀림
잘못된 객체 선택
retrieval 실패
filtering 조건 오류
최종 값 선택 오류
<img width="1215" height="473" alt="image" src="https://github.com/user-attachments/assets/4f276371-5422-4d8e-86ae-7b4162b82b72" />

1. Environment Contract Layer = “환경 규칙 설명서”

에이전트 시작 전에:
“여기선 이런 규칙으로 행동해야 함”
을 추가로 알려줌.

상품권 최대 3개
왕복 항공편은 같이 제출
구매 전 옵션 선택 필수

2. Procedural Skill Layer = “공략집”

과거 성공 trajectory에서 자주 통하는 전략을 저장해놨다가 현재 task와 비슷한 스킬 하나를 꺼내줌.

DB 수정 전에 먼저 preview SELECT 해라
grep -c 결과는 파일별 count라 합산 필요
WebShop에선 petite/tall도 옵션일 수 있음

3. Action Realization Layer= “실행 직전 검사기”

모델이 action 생성하면 바로 실행하지 않고:
“이거 환경에서 실제 가능한 행동인가?”
를 체크함.

좌석 없는 항공편 예약 → 차단
옵션 선택 없이 buy now → 차단
SQL 문법 살짝 틀림 → 자동 보정
<img width="395" height="347" alt="image" src="https://github.com/user-attachments/assets/caa7545d-827d-4352-acf2-f3dc4d3417d4" />


4. Trajectory Regulation Layer= “루프 감시 시스템”

실행 후 trajectory 전체를 보면서:
“지금 같은 행동 반복 중인가?”
를 감시함.

search만 계속 반복
같은 페이지 계속 방문
look/inventory 무한 반복

Evolution
하네스를 사람이 일일이 만든 게 아니라, 실패 trajectory를 보고 계속 자동 개선

고정된(frozen) 모델을 task들에 계속 실행함
→ 에이전트 행동 기록(trajectory) 수집
Codex가 그 trajectory를 읽음
→ 어디서 반복적으로 실패하는지 분석
실패 패턴 발견하면
→ 하네스 레이어 업데이트 제안

<img width="337" height="878" alt="image" src="https://github.com/user-attachments/assets/822d9bfc-5b19-4507-8490-96028ea1df2c" />
<img width="239" height="755" alt="image" src="https://github.com/user-attachments/assets/cdadeaec-4f63-42c5-bfa9-2905c5c7fa8b" />


E. Continual Harness: Online Adaptation for Self-Improving Foundation Agents
===================
클로드코드, 코덱스는 coding harness이고 long-horizon partial-observability decision-making는 없는데 포켓몬,Gemini Plays
Pokémon (GPP) 에서의 하네스를 설계했다

바꾸는 하네스 컴포넌트
📄 시스템 프롬프트 → "배틀에서는 타입 상성 먼저 봐라"
🤖 서브에이전트 → 배틀 전담 AI, 길찾기 전담 AI 등
🔧 스킬 → A* 경로탐색 코드, 데미지 계산기 등
🧠 메모리 → "3번 체육관은 물 타입, 마크네이트 써라" 등 누적 지식

결과보고 이제 agent가 harness refine하는게 아닌
AI가 스스로 자기 하네스를 고쳐가면서 게임함.
[게임 플레이 중]

스텝 1~100:   게임 플레이
스텝 100:     Refiner가 "최근 100스텝 뭐가 문제였나?" 분석
              → 하네스 수정
스텝 101~200: 수정된 하네스로 게임 계속
...
리셋 없이
<img width="1078" height="570" alt="image" src="https://github.com/user-attachments/assets/036701e7-f505-4b69-ba50-501dc5f73f91" />
Refiner가 하는 4가지 작업
매 F스텝마다 최근 trajectory를 보고:

① 실패 탐지
"최근에 같은 장소를 뱅글뱅글 돌았네 (내비루프)"
"툴 호출이 3번 연속 실패했네"
"목표가 50스텝째 안 바뀌네 (정체)"

② 시스템 프롬프트 수정
기존: "배틀에서 공격력 높은 기술 써라"
수정: "배틀에서 타입 상성 먼저 확인하고, HP 30% 이하면 도망쳐라"

③ 서브에이전트 추가/수정/삭제
"맵 이동할 때마다 삽질하네 → 길찾기 전담 서브에이전트 새로 만들기"
"이 서브에이전트는 한 번도 안 불렸네 → 삭제"

④ 스킬 코드 수리
"이 pathfinding 코드가 계속 에러 나네 → 버그 고쳐서 재등록"
"이 시퀀스가 계속 성공하네 → 스킬로 코드화해서 재사용 가능하게"

⑤ 메모리 업데이트
"3번 체육관 클리어했으니까 관련 메모리는 중요도 낮춤"
"4번 체육관 전에 특정 포켓몬 잡아야 한다는 걸 알았으니까 추가
<img width="1015" height="607" alt="image" src="https://github.com/user-attachments/assets/c0cfca7e-4e1a-4688-9d07-c7f5153907bb" />

F. Ctx2Skill
===================
책, 논문, 깃허브 리포와 같은 컨텍스트를 스킬로 바꿈

Challenger 프롬프트
Task 설계 + 루브릭 설계 규칙 두 파트로 나뉨
핵심은 루브릭: "단순 검색으로 풀리면 안 됨", "이진 검증 가능해야 함"
런 루브릭이 있어야 Judge가 외부 정답 없이 채점 가능 → external 피드백 없어도 루프 가능 << 문제 해결하는 핵심
-> 주어진 컨텍스트에 대해 Task + 루브릭 생성

Judge 프롬프트
3단계 강제: 분석 → 검증 → 자기반성
루브릭 단 하나라도 fail이면 전체 0점 (all-or-nothing)
시스템 프롬프트 없이 유저 메시지만 → 다른 에이전트들과 달리 스킬 진화에 관여 안 함, 중립 유지
-> Task 생성된거에 루브릭 기준으로 점수매김

실패시)
Reasoner Proposer 프롬프트

Judge 실패 유형을 6가지로 분류: 내용공백 / 형식오류 / 제약위반 / 추론오류 / 태스크오해 / 시스템프롬프트 미준수
케이스별 분석 금지, 공통 패턴 합성이 핵심
기존 스킬 먼저 확인 → 있으면 새로 만들지 말고 edit

<img width="1137" height="447" alt="image" src="https://github.com/user-attachments/assets/10083848-e1dd-497d-8f93-6bf57e1f38aa" />
Reasoner Generator 프롬프트
Reasoner Proposer이 생성한 diagnosis을 실제 SKILL.md로 구체화
구조 4파트 강제: 사전 체크리스트 / 응답 절차 / 자기검증 / 함정
YAML 프론트매터 두 필드만 (skill_name, skill_description)

성공시)
Challenger Proposer 프롬프트
Reasoner Proposer랑 반대
"왜 Reasoner가 쉽게 풀었나?" → "다음엔 더 어렵게 내는 전략" 도출
진단 8가지 체크: 태스크 유사성 / 루브릭 관대함 / 단순성 / 다양성 부족 등

Challenger Generator 프롬프트
Reasoner Generator랑 거의 동일 구조
차이점: 출력 구조가 "할 일 / 좋은vs나쁜 패턴 예시 / 함정" → 태스크 생성 전략 중심
<img width="1089" height="608" alt="image" src="https://github.com/user-attachments/assets/f3ab08d0-1e3d-437a-aa7d-2fe912b0abe6" />


## Ctx2Skill 구현 방식 딥다이브

---

### 1. 전체 파이프라인 (알고리즘 흐름)

```
Input: Context C, iterations N=5, tasks per iter M=5

초기화:
  S_R^0 = ∅  (Reasoner 스킬셋, 빈 상태로 시작)
  S_C^0 = ∅  (Challenger 스킬셋)
  Q_hard = []  (하드 프로브셋)
  Q_easy = []  (이지 프로브셋)

for i = 1..N:
  ① Challenger(C, S_C^{i-1}) → {(t_m, R_m)} × M개 태스크+루브릭
  ② Reasoner(C, S_R^{i-1}, t_m) → a_m  (M개 답변 생성)
  ③ Judge(t_m, R_m, a_m) → y_m ∈ {0,1}  (루브릭 전체 pass면 1)
  
  ④ 실패케이스 F_i = {m : y_m = 0}
     → Reasoner Proposer(F_i, S_R^{i-1}) → 진단 d_R
     → Reasoner Generator(d_R, S_R^{i-1}) → S_R^i
  
  ⑤ 성공케이스 P_i = {m : y_m = 1}
     → Challenger Proposer(P_i, S_C^{i-1}) → 진단 d_C
     → Challenger Generator(d_C, S_C^{i-1}) → S_C^i
  
  ⑥ 프로브 누적:
     Q_hard += argmin_{m∈F_i}(루브릭 통과율)  # 가장 어렵게 실패한 태스크
     Q_easy += argmin_{m∈P_i}(루브릭 수)      # 가장 쉽게 성공한 태스크

// Cross-Time Replay
for i = 1..N:
  ρ_h(i) = (Q_hard에서 S_R^i로 맞힌수 + 1) / (|Q_hard| + 1)  # Laplace smoothing
  ρ_e(i) = (Q_easy에서 S_R^i로 맞힌수 + 1) / (|Q_easy| + 1)

i* = argmax_i [ ρ_h(i) × ρ_e(i) ]
return S_R^{i*}
```

---

### 2. 각 에이전트의 실제 프롬프트 구조

#### Challenger
```
System: 너는 컨텍스트 전문가야
Input:
  - Context C (전체 문서)
  - S_C^{i-1} (Challenger 스킬셋)
  
Output: M개의 태스크, 각각에 binary 루브릭 리스트
  예시 루브릭:
    - "답변에 3단계 절차가 명시되어 있는가?"
    - "컨텍스트의 조항 3.2를 인용하는가?"
    - "수치가 소수점 2자리인가?"
  
핵심 제약: "컨텍스트 표면만 paraphrase하면 pass되는 태스크 금지"
         "룰을 귀납적으로 이해해야 풀 수 있어야 함"
```

#### Judge (엄격한 all-or-nothing)
```
3단계 채점:
  Step 1: 각 루브릭의 요구사항 분석
  Step 2: 루브릭별 pass/fail 개별 판정
  Step 3: 자기 반성 (놓친 게 있는지)

y_m = 1  오직 모든 루브릭이 pass일 때만
→ 루브릭 11개 중 10개 맞아도 y_m = 0
```

#### Proposer (진단 전문가)
```
Reasoner Proposer:
  Input: 실패 케이스들 F_i + 현재 S_R^{i-1}
  출력 형식 (structured):
    - action: "add" | "merge"
    - target_skill_name: str
    - description: str (무슨 지식이 빠졌는가)
    - justification: str (왜 이 케이스들이 그 증거인가)
  
  핵심: 케이스별 진단 X → 여러 케이스의 공통 패턴 합성
  예: "실패 4건이 모두 예외조항 처리 미흡 → add skill: 예외처리 프로토콜"
```

#### Generator (스킬 구체화)
```
Input: Proposer의 진단 + 현재 스킬셋
Output: 완전한 새 SKILL.md (기존 스킬 보존 + 신규/병합 반영)

규칙:
  - 관련없는 기존 스킬은 그대로 유지
  - add → 새 섹션 추가
  - merge → 기존 섹션에 통합
```

---

### 3. Cross-Time Replay 구현 디테일

**왜 마지막 iteration 스킬을 쓰면 안 되냐?**

실험 결과 GPT-4.1 기준:
| Iter | Solving Rate |
|------|-------------|
| 1 | 15.9% |
| 2 | 15.5% |
| 3 | 15.3% |
| 4 | 14.9% |
| 5 | 14.7% |
| **Cross-Time Replay** | **16.5%** |

→ iter 늘어날수록 오히려 나빠짐. Challenger가 점점 극단적 태스크만 내니까 Reasoner 스킬이 그것에 과적합됨

**선택 로직**:
```python
# Laplace smoothing으로 빈 프로브셋 edge case 처리
rho_hard = (hard_solved + 1) / (len(Q_hard) + 1)
rho_easy = (easy_solved + 1) / (len(Q_easy) + 1)

score = rho_hard * rho_easy  # 곱셈 중요!
# 왜 덧셈 아니고 곱셈?
# → 덧셈이면 hard=0%, easy=100%도 score=1.0
# → 곱셈이면 한쪽 0%면 score=0 → 극단적 스킬 자동 제외
```

**프로브셋 구성 방식**:
- `Q_hard`: 매 iter마다 "루브릭 통과율 최저인 실패 태스크" 1개 추가 → 5 iter면 최대 5개
- `Q_easy`: 매 iter마다 "루브릭 수 최소인 성공 태스크" 1개 추가

즉 N=5면 Cross-Time Replay에서 각 후보 스킬셋을 최대 10개 태스크로 재평가하는 것

---

### 4. 실제 운용 수치 (500개 context 기준)

| 설정 | 값 |
|---|---|
| N (iterations) | 5 |
| M (tasks per iter) | 5 |
| Judge 모델 | GPT-5.1 (고정, 모든 백본 공통) |
| 총 API 호출/context | ~100회 이상 (5iter × 5task × 여러 에이전트) |
| 총 비용 | ~$30K (500 context 전체) |
| 선택된 iter 분포 | 대부분 1~2번째 iter가 선택됨 (Cross-Time Replay 결과) |

---

### 5. 스킬 전이 실험 결과 (흥미로운 부분)

| 스킬 생성 백본 | 추론 백본 | Solving Rate |
|---|---|---|
| GPT-4.1 | GPT-4.1 | 16.5% (자체) |
| GPT-5.1 | GPT-4.1 | 16.1% (거의 동등) |
| GPT-4.1 | GPT-5.1 | 23.1% (+2.0%) |
| GPT-5.1 | GPT-5.1 | 25.8% (자체) |

**해석**: 강한 모델이 만든 스킬 → 약한 모델에 꽂아도 잘 작동. 반대는 약함. 스킬 자체가 일종의 **distilled knowledge** 역할을 함.

---

### 6. 아키텍처 관점 핵심 설계 결정

**왜 Proposer/Generator 분리했나?**
- 합치면 -0.5% 하락
- 진단(what & why)과 구체화(how)를 분리해야 품질 높아짐
- LLM이 한 번에 "문제 분석 + 스킬 작성" 하면 진단이 얕아짐

**왜 두 사이드 스킬을 서로 안 보여주나?**
- `S_R`과 `S_C`는 서로 격리됨
- 만약 Reasoner가 Challenger 스킬을 보면 → 시험 답지 보는 꼴
- 엄격한 adversarial 구조 유지가 핵심

**왜 both sides를 업데이트하나 (Loser-Only 대비 +0.5%)?**
- Challenger도 성공 케이스 보고 "다음엔 이 방향 피해야지" 학습
- 한쪽만 업데이트하면 Challenger가 진화 안 해서 adversarial pressure 약해짐

---

네 HarnessBuilder에 이걸 적용하면 어떻게 되냐면:
- **Context C** = 보험약관 문서
- **Skill** = 취약조항 탐지 하네스 (SKILL.md)
- **Task+Rubric** = "이 약관에서 면책조항을 찾고, 루브릭: 조항번호 명시/법적 근거 인용/소비자 불이익 서술"
- **Judge** = 전문가 레이블 or 금감원 분쟁사례로 ground truth 검증

이 구조 그대로 팀밸런스 하네스 자동 진화 파이프라인으로 쓸 수 있어. 관심 있으면 이어서 얘기해볼까?
G. Cos-Play
<img width="894" height="729" alt="image" src="https://github.com/user-attachments/assets/aa5ed5a9-f08a-4983-8985-ca7a1d38244b" />
<img width="542" height="759" alt="image" src="https://github.com/user-attachments/assets/a9ee222a-1f52-4ddc-92d7-812b813036e7" />
<img width="523" height="784" alt="image" src="https://github.com/user-attachments/assets/960855bb-e3f8-4143-bc33-5a78dd014f3a" />
<img width="495" height="511" alt="image" src="https://github.com/user-attachments/assets/921f23c5-b80d-45b9-a5fe-7c30c0ee8801" />

H. Meta Context Engineering
<img width="1043" height="609" alt="image" src="https://github.com/user-attachments/assets/1ca99bf0-e390-4756-a9b4-dc8b9d6df901" />

I. SkillClaw

J. AutoHarness
